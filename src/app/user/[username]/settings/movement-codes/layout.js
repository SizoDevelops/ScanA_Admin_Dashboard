export const metadata = {
  title: "Movement Codes | Settings | ScanA",
  description: "By Sizo Develops",
};

export default function LocationLayout({ children }) {
  return <html lang="en">{children}</html>;
}
