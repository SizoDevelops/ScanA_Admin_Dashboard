export const metadata = {
    title: 'Reset Password | ScanA',
    description: 'By Sizo Develops',
  }
  
  
  export default function ForgotPasswordLayout({ children }) {
    return (
    
        <div > 
          {children}
          </div>

  
     
    )
  }