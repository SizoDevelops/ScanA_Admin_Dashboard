export const metadata = {
    title: 'Login | ScanA',
    description: 'By Sizo Develops',
  }
  
  
  export default function LoginLayout({ children }) {
    return (
      <html lang="en">
        <body > 
          {children}
          </body>
      </html>
  
     
    )
  }