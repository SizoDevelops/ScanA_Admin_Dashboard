import Permissions from '@/components/Settings/permissions';
import React from 'react';

const Page = () => {
    return (
        <div>
            <Permissions/>
        </div>
    );
}

export default Page;
